import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Customers',
};
const Page = () => {
  return (
    <main>
      This is customers page!
    </main>
  );
};

export default Page;